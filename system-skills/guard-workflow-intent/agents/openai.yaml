interface:
  display_name: "工作流意图守卫"
  short_description: "自动确认目标、边界与约束，并严格校验工作流生成和调整结果"
  default_prompt: "Use $guard-workflow-intent to clarify and validate this workflow-building request."
policy:
  allow_implicit_invocation: true
